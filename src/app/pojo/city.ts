export class City {
  id: string;
  name: string;
  district: string;

  constructor (id: string, name: string, district: string) {
    this.id = id;
    this.name = name;
    this.district = district;
  }

  

}