interface FirestoreBoolean {
    booleanValue: boolean;
}

interface FirestoreString {
    stringValue: string;
}

interface FirestoreInteger {
    integerValue: number;
}

interface FirestoreDouble {
    doubleValue: number;
}